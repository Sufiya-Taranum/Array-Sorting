#include <stdio.h>
#include <assert.h>
#include "Day4_header.h"

int main()
{
    int a[4]={40,60,10,30};
    print_array(a,4);
    sort_asscending_order(a,4);
    sort_descending_order(a,4);
    return 0;
}
